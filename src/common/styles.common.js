import styled from 'styled-components';

export const styledWrapper = styled.div`
  background: rgba(0, 0, 0, 0.1);
`;

export const styledDiv = styledWrapper.extend`

`;

export const styledImg = styled.img`
  width: 100%;
`;
